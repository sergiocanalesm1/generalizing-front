/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef, useState } from "react";
import { useGLTF } from "@react-three/drei";
import { useFrame } from "@react-three/fiber";
<<<<<<< HEAD
const articleModelUrl = `${process.env.REACT_APP_MODELS_BUCKET}/glbs/newspaper2.glb`;
=======
import articleModelUrl from "./models/newspaper2.glb";
>>>>>>> 1f2d62f8ad4f5f3a1f2a0b97441b43aa350a1362

export default function Newspaper(props) {
  const { nodes, materials } = useGLTF(articleModelUrl);
  const mesh = useRef();
  const [hovered, setHovered] = useState(false);

  useFrame((state, delta) => {
    if (hovered) {
      mesh.current.rotation.y += delta;
    } else if (mesh.current.rotation.y > 0.01) {
      mesh.current.rotation.y -= delta;
    }
  });

  return (
    <group
      {...props}
      ref={mesh}
      dispose={null}
      onPointerOver={() => setHovered(true)}
      onPointerOut={() => setHovered(false)}
    >
      <mesh
        geometry={nodes.np_mid.geometry}
        material={materials["newspaper_page-0001"]}
        scale={5}
      />
    </group>
  );
}

useGLTF.preload(articleModelUrl);
